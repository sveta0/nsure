{
  "name": "nsure",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ sveta0/nsure",
    "create-local": "graph create --node http://localhost:8020/ sveta0/nsure",
    "remove-local": "graph remove --node http://localhost:8020/ sveta0/nsure",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sveta0/nsure"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
